
const Button = ({text}) => {
    return (
        <button className='h-12 bg-red-600 w-44 rounded-full font-extrabold text-white'>
            {text}
        </button>
    );
};

export default Button;